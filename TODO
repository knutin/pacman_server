TODO:
 * Timeout between 'move' calls, abort game if timeout is reached without a move
 * Allow ghosts to compute moves in parallel
 * When a web client disconnects, remove it from the token2pid table, link and trap_exit
 * When a game client disconnects, disconnect web clients and clean the token2pid table
 